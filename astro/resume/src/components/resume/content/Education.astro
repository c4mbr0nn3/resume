---
import ExternalLink from '../../ExternalLink.astro'
import Title from './Title.astro'

const { education, t } = Astro.props
---

<section>
  <Title title={t.education} />
  {education.map((item: any) => (
    <div class="mb-5">
      <div class="flex flex-col justify-between sm:flex-row sm:items-center">
        <h3 class="text-lg font-semibold">
          <ExternalLink href={item.url} label={item.institution} />
        </h3>
        <p class="text-sm font-mono text-zinc-400 dark:text-zinc-500">
          {item.startDate} - {item.endDate}
        </p>
      </div>
      <p class="font-light text-zinc-600 dark:text-zinc-400">{item.studyType} in {item.area}</p>
    </div>
  ))}
</section>
